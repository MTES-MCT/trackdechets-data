version: 2
models:
  - name: transporter_receipt
    columns:
      - name: id
        tests:
          - unique
        description: Clé primaire
        data_type: string
      - name: receipt_number
        data_type: string
      - name: validity_limit
        data_type: datetime64(6, 'europe/paris')
      - name: department
        data_type: string
